<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Remove deprecated GET_ACCOUNTS permissions -->
    <!-- <uses-permission android:name="android.permission.GET_ACCOUNTS" /> -->
    <!-- <uses-permission android:name="android.permission.GET_ACCOUNTS_PRIVILEGED" /> -->

    <!-- Declare hardware features -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.wifi"
        android:required="false" />

    <!-- Content rating for Play Store -->
    <meta-data
        android:name="content_rating"
        android:value="T" />
    <meta-data
        android:name="target_audience"
        android:value="general" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        tools:targetApi="31">

        <!-- YouTube API compliance metadata -->
        <meta-data
            android:name="youtube_api_compliance"
            android:value="true" />
        <meta-data
            android:name="content_filtering_enabled"
            android:value="true" />
        <meta-data
            android:name="no_content_download"
            android:value="true" />

        <!-- File provider for secure file sharing -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Main activity -->
        <activity
            android:name=".YouTubeActivity"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <!-- App shortcuts for Android 7.1+ -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <!-- YouTube Player Activity -->
        <activity
            android:name=".YouTubePlayer_Activity"
            android:exported="false"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:screenOrientation="sensor"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen" />

        <!-- Search Activity -->
        <activity
            android:name=".SearchVideos.SearchActivity"
            android:exported="false"
            android:parentActivityName=".YouTubeActivity"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".YouTubeActivity" />
        </activity>

    </application>

    <!-- YouTube API service requirements -->
    <queries>
        <package android:name="com.google.android.youtube" />
        <package android:name="com.android.vending" />
        
        <!-- Intent filters for YouTube content -->
        <intent>
            <action android:name="com.google.android.youtube.api.service.START" />
        </intent>
    </queries>

</manifest>
